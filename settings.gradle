pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // NewPipe Extractor é publicado no JitPack
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "NexusMusicApp"
include ':app'
